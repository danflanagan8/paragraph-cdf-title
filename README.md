# README #

Paragraphs don't have titles. When a Paragraph gets exported to Content Hub, it is labeled "(Untitled)" by default. 
This module provides a configuration form that allows users to create meaningful titles for Paragraphs during export
using fields and tokens.